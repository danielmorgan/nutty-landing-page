---
const { title, subtitle, illustration, illustrationAlt } = Astro.props;
---

<div class="flex flex-col justify-between gap-4 rounded-xl bg-white p-4 md:p-6">
  <div>
    <h2 class="text-nutty-500 text-2xl text-pretty md:text-3xl">
      {title}
    </h2>
    <h3
      class="text-grey-800 mt-4 text-lg font-semibold text-balance md:text-xl"
    >
      {subtitle}
    </h3>
    <p class="text-grey-800 mt-4 text-base md:text-lg"><slot /></p>
  </div>
  <div class="flex items-end justify-center">
    <img src={illustration} alt={illustrationAlt} class="w-2/3" />
  </div>
</div>
